server:
  port: 8080
spring:
  config:
    location: classpath:/database.yml
  datasource:
    url: jdbc:mysql://localhost:3308/chat_jwt
    username: root
    password: 12345
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: MySQL
    database-platform: org.hibernate.dialect.MySQL8Dialect